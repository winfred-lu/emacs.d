Emacs Envionment
=============

Installation
-----------

	$ git clone git://github.com/winfred-lu/emacs.d.git ~/.emacs.d
	$ cd ~/.emacs.d
	$ git submodule init
	$ git submodule update

Optional:

	$ ./vendor/compile.sh

Usage
-----------

	$ emacs
